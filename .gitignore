# 真实库 data/real_universe 已入库：其中的价格修复（close_raw 反推、688 单位修正、
# total_share 两股修正）无法用脚本复现，必须留档。
# 以下仍不入库：
#   data/*/prices.parquet.bak*、prices.parquet.full   修复过程中的备份
#   data/demo、smoke_db、correctness_db、vi_db         可用脚本重建的库
#     python -m vi_system.cli demo   生成 demo
#     build_demo_store()             生成 smoke_db / correctness_db
#     tests/fetch_real.py            重抓 vi_db / real_universe
#   data/*/*.log、smoke_decisions.jsonl                运行时日志
/data/*/*.parquet.bak*
/data/*/prices.parquet.full
/data/demo/
/data/smoke_db/
/data/correctness_db/
/data/vi_db/
/data/*.log
/data/*/*.log
/data/smoke_decisions.jsonl

# 运行时日志
out/*.log

# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/

# 系统
.DS_Store

# 净值曲线缓存（regen_nav_curve.py 中间产物，可用 --dump 重建）
out/_navcache/
# 抓取脚本 stdout 重定向
/data/*/*.out

# close_adj 修复产生的 54MB 全量备份，不入库（可用 fix_adj_local.py 回滚 close_adj_old 列替代）
data/real_universe/prices_adj_backup_*.parquet
