# 2026-09-10 工作日志

## 今日持仓建议（修复 gen_position_advice.py 后生成）
- 新增 `tests/gen_position_advice.py`：按 verdict 分组（已到买点/合理区间/已到卖点）输出持仓明细 + 100万资金手数参考。
  - 列：代码/名称/行业/权重/Vz/Qz/Sz/总分/市值/g*/中枢/买点/卖点/熊市下行/现价/手数。
  - **两个 bug 已修**：(1) `_fmt` 调用参数顺序错（`_fmt(r,c,val)` → 应为 `_fmt(k,val)`），导致数字/权重全错；
    (2) `code` 列因 `extra=scored.set_index("code")` 后不在列里 → `row.get("code")` 返回 None，改用 `r["code"]`；
    (3) `weight` 只在组合 pf 里、不在 scored → 用 `r["weight"]`。
- 生成 `out/持仓建议-2026-09-10.md`：宇宙 780 → 排雷 393 → 过门 64 → 组合 30 + 现金 10%。
  - **已到买点 21 只 + 合理区间 9 只**，已到卖点 0 只（verdict 回灌生效）。
  - 行情口径 2026-09-07（库内最新交易日；9/8、9/9 缺失未抓）。
  - 报告头部带 ⚠️ 提示 goodwill_to_equity 0 覆盖、排雷形同虚设。

## close_adj（后复权）数据污染 —— 重大数据质量 bug
- 用户问"为什么已实现收益(含分红)比价格收益小"。根因：**`close_adj` 不是干净后复权序列**。
- 口径确认（`gen_backtest_report.py` 170-175 + `engine.py` 538-567）：价格收益=close_raw(不复权)，含分红=close_adj(后复权)。定义上含分红应≥价格收益。
- 中国动力(sh600482) 实测：close_raw +80.5% / close_adj +75.9%（后者反低）。
- 证据：复权因子 `close_adj/close_raw` **每个交易日漂 1%~6%**（茅台/平安/中国动力同现），正常后复权因子只在分红/送转日跳变、平时恒定。说明 close_adj 调整因子在漂移，把含分红收益凭空带偏（中国动力段约 -4.6pp）。
- **污染面大**：engine 收益列与回测 NAV 全用 close_adj（代码明说"与 NAV 同口径"）→ 整个回测净值(年化11.7%/13.9%)也被漂移偏置。close_raw 9-08 修过已可信，close_adj 没动过。
- 来源：westock._fetch_kline(adj="hfq") 直接落库，后复权因子锚点不稳。
- 修复方向：(a) 从干净源重抓/固定锚点重算 close_adj；(b) 或含分红改 = 价格收益 + 区间每股分红/建仓成本（但 facts 里 sh600482 dividend 为 0 行，分红数据也可能缺）。待用户决定是否动手。

## close_adj 修复已实施（tests/fix_adj_local.py）
- **根因细化**：脏 `close_adj/close_raw` 是**负一阶自相关的均值回复振荡 + 缓慢虚假漂移**（中国动力 ratio 从 2.83 平滑下滑到 2.66，8 个月 -6%）。后复权因子本应只在除权日跳变。腾讯 fqkline(web.ifzq.gtimg.cn) 现被沙箱封(501)，eastmoney push2his 也断连——网络源不可达。
- **gtimg 金标准验证**：已抓的 153 只 gtimg 同源 `qfq/raw` 因子干净（日跳变仅 0.056%），中国动力该因子平滑**上升**(0.986→0.995)，据此算含分红=+82.1% > 价格+80.5%（正确）。而 westock 脏数据反向下滑，把含分红压到+75.7%。
- **最终方案（纯本地，不依赖网络）**：
  - 153 只金标准：clean_factor = K × gtimg_factor，K=median(ratio/gtimg_factor)，把干净阶梯因子映射回 westock 量纲（含分红与真实分红一致）。
  - 其余 649 只（无金标准）：全期中位数作常数因子（漂移/抖动一起消），含分红≈价格收益（不再倒置），回测退化为裸价收益（诚实保守）。
  - 锚点常数在收益率中抵消，不影响回测收益方向。
- **落地**：`tests/fix_adj_local.py --apply` 已覆盖 prices.parquet.close_adj；原值存 `close_adj_old` 列；另备 `data/real_universe/prices_adj_backup_20260910.parquet`。
  - 校验：日跳变>1% 从 17.32%→1.34%（剩余为真实除权跳变）；中国动力含分红 +82.1% ≥ 价格 +80.5% ✅。
- **连锁影响**：回测 NAV 之前建立在脏数据上，三档回测报告 + nav-curve 正在后台(--holdings 30/5/3 + nav dump/plot)重跑，完后再刷新 README 数字。
- 教训：westock 的 hfq 后复权不能用；腾讯 fqkline 与 eastmoney 在本沙箱不稳，金标准因子若需补抓须避开限流（之前 12 并发触发封禁）。

## 更正（同日后续）：close_raw 本身也大面积损坏 —— 之前的假设错了
- 重跑三档回测后 Top5 年化飙到 +76%、鄂尔多斯 90 倍暴涨，追查发现**根因不是 close_adj，而是 close_raw（不复权价）本身脏**。
- 量化：802 只里 **42 只 close_raw 出现负值**（股价不可能为负），其中 **12 只在回测窗口(2017-05~2026-05)内有 2007 行非正价格**；另 36 只 close_raw 在(0,1)却又能冲到 3+（也脏）。共约 78 只明确损坏。
- 根因在数据源：westock `_fetch_kline(adj=None)` 返回的 `last`（不复权）对部分票/日期直接给负值/垃圾值；`westock.py:351` 只 `dropna` 没拦负值，脏值直接落库。
- 中国动力(sh600482) 的 close_raw 是干净的（7.38~50.93），只有 close_adj 有噪声；且它在金标准 153 内 → 有干净 gtimg 因子可精修。
- **我的 close_adj 修复（fix_adj_local.py）反而更糟**：它假设 close_raw 干净、令 close_adj=close_raw×常数，对 close_raw 损坏的票把 90 倍爆炸原样搬进了 close_adj。已**撤销**（close_adj 还原为原始值，删 close_adj_old 列），数据回到修之前的安全态。撤销后中国动力含分红<价格 现象重现（已知脏现象，但非灾难）。
- 覆盖情况：负价 42 只中 14 只在金标准153（可修），28 只非金标准（无干净源，只能隔离或重抓）；金标准153自身也有 14 只 close_raw 含负值。
- **结论**：要彻底解决需全量重抓干净行情（gtimg 直连，但此前撞过腾讯 IP 封禁）；快速稳妥方案是「隔离 ~78 只损坏票 + 用金标准153 重建干净 close_adj」。README 当前仍是旧脏数字（da65f03），out 三档报告现为爆炸版（勿提交）。
- 当前 git：prices.parquet 已本地撤销但未提交；origin/main 仍停在爆炸版(4047389)。待用户定方向后再落地提交。

## 全量重抓干净行情（用户拍板：先"隔离+金标准修复"，后改"重新做，把数据做对"）
- 用户先选方案 A（隔离 ~78 只损坏票 + 用金标准 153 重建 close_adj），随后改为「重新做吧。把数据做对」→ 采用**全量重抓**。
- 脚本 `tests/rebuild_prices_gtimg.py`：gtimg 直连抓 raw(不复权)+hfq(后复权) 双序列，按 (code,date) 对齐生成新 (close_raw, close_adj)；带断点续传检查点 `prices_gtimg_clean.parquet` + 完全失败票隔离到 `prices_excluded.parquet`。已抓 92 只（旧端点），零负值。
- **端点坑（重要）**：gtimg `appstock/app/fqkline/get` 端点已被腾讯**下线**，现在一律返回 **HTTP 501**。
  - ⚠️ 之前 3 并发版"卡死不动"被误判为限流/封禁，**实为端点失效**：每请求 30s 超时 ×4 次重试，40 只/批要 20+ 分钟才打印一行。
  - 可用替代：`https://web.ifzq.gtimg.cn/appstock/app/newfqkline/get?param=<code>,day,<beg>,<end>,2000,<fq>`（fq ∈ '','qfq','hfq'），返回 `day/qfqday/hfqday`，数据干净无负值。
  - `appstock/app/kline/kline` 亦可用，但只有不复权 day，无复权序列。
  - **分页**：带复权序列大 count 会截断（2000 只回 640 行），必须翻页；翻页后 2014→今 ≈2950 行/票。
- 并发：用户要求逐步调低 6→3→**2**，REQ_SLEEP=0.15（≈4 QPS）。已冒烟验证 clean_one 对脏票(600295)+金标(600482)+茅台(600519) 均干净。
- **进程存活坑**：`run_in_background` 的抓取进程会随对话轮次结束被回收（多次出现"进程已停、检查点不增长"）。改用 `subprocess.Popen(..., start_new_session=True)` 脱离会话启动，才可跨轮存活（当前 PID 38167 跑 710 只）。

## 待办
- **Git LFS 尚未落地**：用户要求「上 Git LFS」处理 prices.parquet(52MB) 超 50MB 建议上限。
  brew install git-lfs 在 9/9 夜被中断未完成；本机暂无 git-lfs。需重试安装 + `git lfs install` + 迁 prices.parquet/facts.parquet 入 LFS 并 rewrite。
- 9/8、9/9 两个交易日行情缺失，下次抓行情时补齐（fetch_real.py 已含股本修正钩子）。

## 本轮结论（用户问"能否用腾讯自选股skill避限流"）—— 已穷尽验证
- **腾讯自选股 skill (westock-data-skillhub) 无法做历史回填**：`kline` 硬限 **最多 250 行**，与 `--limit`(哪怕3000) 和 `--start/--end` 无关——日期区间对复权(fq)完全失效，永远返回最近 250 行（约1年）。回测要 2017→2026 约2200行/票，250行不够，且后复权无法分页。**此路彻底堵死。**
- **gtimg(newfqkline) 数据干净但本会话出口被持久限流**：raw 全历史(2001行)干净、hfq 仅最近640行(≈2.5年)。出口按(~80~100请求)限额封锁，连 5 只小批(20请求)也全失败；前台/后台出口当前都封。等待重置时间未知。已干净落盘 **575/802 只**（0负值）于 `prices_gtimg_clean.parquet`。
- **westock 复权因子去噪救不回**：对 close_adj/close_raw 比值做滚动中值(窗口11)只去掉每日抖动（去噪后日跳变0.44%），但**锚点/水平错误去不掉**——中国动力去噪后含分红仍 +76.3% < 价格 +80.5%（倒置重现）。说明 westock 因子是水平错非纯噪声，无法本地修复。
- **干净 close_adj 只能来自 gtimg 因子**：金标准153只(旧端点抓的 `prices_adj_factor.parquet`)有全历史干净因子；新端点 hfq 截断640行。若不能重抓全量因子，全历史含分红数据仅153只完备、422只仅近2.5年、227只不可得。
- **`--merge` 有缺陷（勿再用当前逻辑）**：只覆盖 ckpt 有数据的日期，未覆盖处保留 westock 脏值 → 合并后仍有 3790 行负价、中国动力倒置重现。正确做法须把575只的行整行替换为 ckpt（但非金标422只 hfq 仅640行，2017-2024 区间仍缺 close_adj）。
- **当前安全态**：prices.parquet 已回滚=802只脏原状；universe.parquet 已从 git 恢复=807；gtimg 干净检查点保留575只；`prices.parquet.bak_westock` 为回滚备份。881只之外另存 `prices_excluded.parquet`(227只) 由 merge 生成、已被回滚清掉。
- 可行交付口径（待用户拍板）：(A) 575只干净宇宙隔离227只跑回测（但非金标422只 close_adj 仅近2.5年，全窗口含分红不完全）；(B) 仅153只金标全干净；(C) 等 gtimg 限流重置后后台补齐802只再跑。

## 新发现：新浪(sina)是可用的独立第二源（绕过 gtimg 限流）
- 环境代理 `127.0.0.1:63462` **放行 sina/gtimg、阻断 eastmoney**（push2his.eastmoney.com 返回 000）。故 akshare 东财后端全挂；akshare sina 后端(`stock_zh_a_daily`)封装走 `hisdata_klc2` 压缩格式解析也失败，但**直连 sina 明文端点可用**：
  - 不复权日线：`money.finance.sina.com.cn/quotes_service/api/json_v2.php/CN_MarketData.getKLineData?symbol={sh600482}&scale=240&ma=no&datalen=8000` → 明文 JSON 全历史（上限~6000行，够 2014→今），零负值。
  - 后复权因子：`finance.sina.com.cn/realstock/company/{sh600482}/hfq.js` → `var sh600519hfq={"total":N,"data":[{"d":日期,"f":因子}]}`，用 `close_adj = close_raw × 因子(取该日及之前最近除权日)`。
- 实测校验：中国动力/鄂尔多斯 raw **零负值**（westock 的 -1.56 消失）；中国动力含分红 +41.3% > 价格 +34.6%（悖论修复）；与 gtimg 后复权绝对锚点不同（比值不可直接比），但回测只用时序收益率，因子在除权日正确跳变已验证。
- 意义：**可单源全量重建 802 只**（raw+后复权），不受腾讯限流牵制，比"等 gtimg 解封"更干净。用户拍板：新浪全量建、并发调低避免限流。

## 新浪全量重建已启动 + 重大坑：parquet 检查点被 kill 写坏
- **坑（务必记住）**：后台抓取进程被 ~20min 回收时若**正在写 parquet**，会留下**无 footer 的损坏文件**（`Parquet magic bytes not found in footer`），整个检查点报废、不可恢复。新浪检查点因此损失 225 只进度。
  - **修复**：两个脚本 (`rebuild_prices_sina.py` / `rebuild_prices_gtimg.py`) 均加 `_atomic_write()`：先写 `*.tmp` 再 `os.replace()`；`_load_ckpt/_done_codes` 加 try/except，损坏则隔离为 `.corrupt` 并从头重抓。原子写保证主文件永远是"上一次完整状态"，kill 最多丢当前批次。
- **速度优化**：`datalen=8000` 单只 8.1s（返回体积大），`datalen=4000` 降到 3.5s（≈2013→今，够覆盖回测）。已设 DATALEN=4000、WORKERS=2、REQ_SLEEP=0.5（用户要求低并发避免限流）。
- **进程存活**：本环境 `setsid` 不存在；`Popen(start_new_session=True)` 的新会话进程**仍会被沙箱回收**（实测启动后 <20s 即死，日志只到 `[plan]`）。唯一可靠方式是 Bash 工具 `run_in_background=true`（托管任务，可跨轮但约 20min 被回收），靠检查点续传分段跑。
- **意外收获**：腾讯等待循环此前已推进到 **615/802**（1,566,712 行，约 2547 行/只 → hfq 已是全历史覆盖、非早前 640 行截断），已备份 `prices_gtimg_clean.parquet.bak615` 防写坏。
- **当前双线并行**：新浪 `S1bKpj`（全量 802，主路径/用户选定）+ 腾讯 `96I5yo`（续抓剩余 187，快车道）。任一满 802 即可 merge；两源约 615 只重叠可做交叉校验。

## 行情重建完成 ✅ + 关键判定：新浪复权可信、腾讯复权不可信
- **判据（不依赖第三方）**：比较「含分红收益 − 价格收益」(gap)。gap<0 即物理不可能（分红不会让收益变少）。
  - 两源**价格收益完全一致**（中位差 0.000pp）→ `close_raw` 两源都可靠。
  - 新浪 gap：**0% 倒置**，中位 +32.2pp（方向永远对）。
  - 腾讯 gap：**16.6% 倒置** → 腾讯 `close_adj` 与 westock 同源病灶（复权锚点错），**只能信 close_raw，不能信 close_adj**。
- **限流规律**：新浪约 **500~600 次请求触发一次封锁**，冷却约 2 小时；单线程 1.3~1.7s/只时不触发。故改「只补缺口」(`--only-missing`，跳过腾讯已覆盖的票) 最小化请求量。
- **最终覆盖**：新浪 764/802（166 只缺口 + 296 只升级），腾讯补 38 只且全部过 `gtimg_gate` 复权体检（判据 0≤gap≤150pp；未通过则 `close_adj=close_raw` 退化为保守口径：含分红≈价格，诚实保守）。
- **落盘**：`tests/merge_clean_prices.py --apply` → prices.parquet **802 只 / 1,997,344 行 / 零负价**（原 11,496 行负价）。备份 `prices.parquet.bak_preclean`。
  - 丢弃 2014 年前 108,608 行脏遗留（回测与回看窗口均不涉及），换取全表零负价。
  - 校验：中国动力 价格 +34.6% / 含分红 **+41.3%** ✅（悖论修复）；含分红倒置 1~4/802（0.1~0.5%，原 14/802）。
- **回测恢复正常**：默认档(30只) 年化 **12.51%**、波动 15.16%、夏普 0.94、最大回撤 −13.40%；对等权 −1.25%、对沪深300 +9.27%。（脏数据版曾虚高到 Top5 +68%/Top3 +83%，已消除）
- 三档报告 + 净值曲线由 `tests/regen_reports_clean.sh` 重跑中。

## 收尾完成（提交 df140d5，已推送 origin/main）
- **最终数据**：802 只 / 1,997,344 行 / 零负价。新浪 764 只 + 腾讯 38 只（全部过 `gtimg_gate`）；prices.parquet 列 = code/date/close_raw/close_adj/volume/amount/total_share/mktcap（无 close_adj_old 残留）。
- **README 效果速览（最终，从三档报告 final 数据取）**：

| 档位 | 年化收益 | 超额(对沪深300) | 区间总收益 | 最大回撤 | 夏普 | 胜率(对沪深300) | 持仓 |
|---|---|---|---|---|---|---|---|
| Top30 | +14.4% | +11.2% | +250.6% | −11.23% | 1.05 | 74.1% | 29.3 |
| Top5 | +10.1% | +6.8% | +144.3% | −17.26% | 0.70 | 66.7% | 5.0 |
| Top3 | +12.4% | +9.2% | +197.3% | −30.66% | 0.71 | 55.6% | 3.0 |
| 等权全宇宙 | +14.2% | +10.6% | +243.6% | −20.46% | 0.78 | 77.8% | ≈780 |
| 沪深300 | +3.2% | — | +34.6% | −38.2% | — | — | — |

- **结论翻转**：旧数据下 Top5 最亮（+13.9%）、貌似"越集中越赚"；干净数据下 **Top30 全面最优**（收益/夏普/回撤三项第一）。集中到 3 只回撤放大到 −30.66%。**"集中更优"是脏数据制造的假象。**
- **口径坑（重要）**：项目里三套基准口径并存，数字会不一致，勿混用——① 报告 `gen_backtest_report --holdings N`（**会自动放宽单票/行业上限**）；② CLI 默认 `--max-holdings N`；③ nav 缓存 `regen_nav_curve.py --dump`（传了 `benchmark`）。同一"5 只"三者 CAGR 分别 10.1% / 7.5% / 7.5%。README 以**报告**为准；超额由报告「区间总收益」与沪深300 总收益反解（几何年化），胜率由报告逐期「组合收益」对沪深300 同区间收益比对算出。
- **环境教训**：托管后台任务 ~20min 必被回收；长回测（单次 ~9min）并行 2~3 个容易被连带杀，**单任务串行最稳**。`_navcache` 只由 `regen_nav_curve.py --dump` 写，`regen_charts.py` 不写。
- **待办**：① 新浪冷却后补最后 38 只 → 全 802 单源；② Git LFS 仍未装（prices 52MB，超 50MB 建议值但可推）。

## 最终闭环（提交 46b14e6 + 22462fb，已推送 origin/main）
- **新浪解封后补齐**：20:38 探测已恢复 → 补抓最后 38 只（含平安银行 sz000001、万科A sz000002）→ **802/802 全单源新浪**，`merge_clean_prices.py --apply` 重合并，`prices.parquet` = 1,997,344 行 / 零负价 / 列无冗余。
- **数据源变化引起的微调**：三档报告重出后 Top30 14.4→**14.5%**、Top5 10.1→**9.9%**、Top3 12.4→**12.1%**；等权基准区间 243.6→245.2%。
- **年化口径修正**：报告年化不是按 9 年，而是 **9.3072 年**（2017-05-15→2026-09-07），据此反解的对沪深300 年化超额 = Top30 **+10.9%** / Top5 **+6.5%** / Top3 **+8.6%**；胜率 74.1% / 66.7% / 55.6%（等权 77.8%）。
- **净值缓存**：`regen_nav_clean.sh`（只补比 prices 旧的档，幂等）跑通 4 档 → `--plot` 重画 `nav-curve.png`。nav 缓存口径 cagr：30=12.97%、5=6.23%、3=9.52%、等权 bench=14.23%（与报告口径不同，属已知三套口径之一）。
- **遗留**：Git LFS 未装（prices 52MB）。项目基准三口径并存的乱象未根治，README 已声明以报告为准。
